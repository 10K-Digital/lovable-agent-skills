{
  "description": "Lovable plugin hooks for auto-sync, auto-push and auto-deploy",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${PLUGIN_ROOT}/hooks/auto-sync.sh",
            "timeout": 30,
            "description": "Automatically sync with GitHub before a Codex session (clean main branch only)"
          }
        ]
      }
    ],
    "Start": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/auto-sync.sh",
            "timeout": 30,
            "description": "Automatically sync with GitHub before starting work (pulls latest changes on main branch)"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/auto-push.sh",
            "timeout": 30,
            "description": "Automatically commit and push changes to GitHub when auto-push is enabled"
          }
        ]
      }
    ]
  }
}
